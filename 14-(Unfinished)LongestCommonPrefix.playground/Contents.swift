import Cocoa

class Solution {
    func longestCommonPrefix(_ strs: [String]) -> String {
        guard strs.count > 2 else { return "" }
        
        var prefix = strs.first!
        
        
    }
}
